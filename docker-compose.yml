version: '3'

volumes:
  db:

services:
  db:
    container_name: db
    image: postgres:13.10
    ports:
      - 5432:5432
    volumes:
      - db:/var/lib/postgresql/data/
    env_file:
      - .env
